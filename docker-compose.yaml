version: '3.8'
services:
  api-gateway:
    build: ./api-gateway
    ports:
      - "3000:3000"
    depends_on:
      - user-service

  user-service:
    build: ./user-service
    ports:
      - "4000:4000"